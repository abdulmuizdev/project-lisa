# Webcam Capture

Run the app from the project root:

```bash
cd /Volumes/MacSSD/Projects/SaaS/ProjectLisa/project-lisa
source .venv/bin/activate
python webcam_capture.py
```

**Controls:** `s` — save snapshot · `q` or `ESC` — quit
